# Anthropic — used by /api/chat (server-only; never exposed to browser)
ANTHROPIC_API_KEY=

# Postgres — dormant in v1 (localStorage-backed). Required when v2 enables accounts.
DATABASE_URL=postgres://user:password@host:5432/db
